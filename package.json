{
  "name": "registry",
  "version": "1.0.0",
  "description": "We register stuff",
  "scripts": {
    "all": "npm run validate && npm run update-ext-badge",
    "update-ext-badge": "npx badge Extensions $(find registry -name extension.json | wc -l) :brightgreen > badges/extensions.svg",
    "validate": "npm run validate-extensions && npm run validate-packs",
    "validate-extensions": "npx ajv validate -d \"registry/**/extension.json\" -s schema/extension.schema.json -r \"schema/*.ref.json\" --allow-union-types",
    "validate-packs": "npx ajv validate -d \"registry/**/pack.json\" -s schema/pack.schema.json -r \"schema/*.ref.json\" --allow-union-types"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "ajv-cli": "^5.0.0",
    "badge-maker": "^4.1.0"
  }
}
