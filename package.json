{
  "name": "registry",
  "version": "1.0.0",
  "description": "We register stuff",
  "scripts": {
    "all": "npm run validate",
    "validate": "npx ajv validate  -d registry/**/plugin.json -s schema/registry.schema.json -r \"schema/refs/*.schema.json\" --allow-union-types"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "ajv-cli": "^5.0.0"
  }
}
